{
  "commands": {
    "run": "cmake --build build -j 8 && ./build/git-monitor",
    "debug:build": "cmake -B build -DCMAKE_BUILD_TYPE=Debug",
    "cmake:preset": "rm -rf build && cmake --preset default && cmake --build build -j 8"
   }
}
